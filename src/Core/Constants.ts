// WINDOW CONSTANTS
const WIDTH   = window.innerWidth;
const HEIGHT  = window.innerHeight;

// ASSET CONSTANTS
const GARBAGE = {                           // Garbage Image Asset Object (Two States)
    open: new Image(),
    close: new Image()
};

// Export Modules
export {
    WIDTH, HEIGHT, GARBAGE
};